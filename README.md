# mlops-internship-task
Minimal MLOps batch pipeline that loads OHLCV market data, computes a rolling-mean trading signal, and writes structured metrics + logs — fully Dockerized for reproducible, one-command execution.
